<section class="wrap prose"><a class="wordmark" href="{{ routes.root_url }}">{{ settings.brand_name | escape }}</a><h1>OST.<br>VON HIER.</h1><p>Der erste Drop entsteht.</p>{% form 'storefront_password' %}{{ form.errors | default_errors }}<label for="Password">Shop-Passwort</label><input type="password" name="password" id="Password" autocomplete="current-password"><button type="submit" class="button">Öffnen</button>{% endform %}</section>
{% schema %}
{
  "name": "Ost Password",
  "settings": [],
  "presets": [
    {
      "name": "Ost Password"
    }
  ]
}
{% endschema %}
